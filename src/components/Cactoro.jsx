/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.0 public/modals/Cactoro.gltf -o src/components/Cactoro.jsx -r public 
*/

import React, { useEffect } from 'react'
import { useGraph } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { SkeletonUtils } from 'three-stdlib'

export function Cactoro({hovered,...props}) {
  const group = React.useRef()
  const { scene, animations } = useGLTF('/modals/Cactoro.gltf')
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone)
  const { actions } = useAnimations(animations, group)
 console.log(actions)
  useEffect(() => {
    const anim = hovered ? "Yes" : "Idle"
    actions[anim].reset().fadeIn(0.5).play()
    return () => actions[anim].fadeOut(0.5)
  }, [hovered, actions])

  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Scene">
        <group name="CharacterArmature">
          <primitive object={nodes.Root} />
          <skinnedMesh name="Cactoro" geometry={nodes.Cactoro.geometry} material={materials.Atlas} skeleton={nodes.Cactoro.skeleton} />
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/modals/Cactoro.gltf')
